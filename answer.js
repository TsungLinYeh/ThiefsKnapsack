/**
 * to get an answer, each item should have two properties weight and worth.
 * I: pack hold X, and array of object for all items
 * O: maxmum worth of the pack can hold
 * C:
 * E:
 * @param {Number} hold - lbs pack can hold
 * @param {[Object]} items - array of objects each obj should have weight and worth properties
 */
const maxWorth(hold, items) => {

}